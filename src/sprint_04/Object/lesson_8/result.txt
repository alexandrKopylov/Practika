Позади ещё одна тема. Через несколько уроков вы приступите к работе над первым проектом — Трекером задач.
Успешно справиться с ним вам помогут полученные знания. А узнали вы очень много! Теперь вам известно, что:

У всех классов в Java есть общий предок — класс Object. Он входит в пакет java.lang.
Наследование от класса Object происходит по умолчанию.
Класс Object может хранить любой объект, его удобно использовать как параметр универсального метода,
который должен принимать объекты разных классов.

Метод equals(Object) по сути аналогичен оператору ==.
Однако при помощи == сравниваются значения примитивных типов, а при помощи equals(Object) — ссылочных.

Каждый объект в программе можно представить в виде некоторого целого числа.
 Процесс вычисления такого числа называется хешированием, а его результат — хешем.
  Метод hashCode() — генерирует хеш для объектов.

Метод toString() возвращает строку, которая представляет объект в виде текста.
Полученное представление должно быть лаконичным и информативным.
Методы класса Object рекомендуется всегда переопределять.

Уже совсем скоро вы сможете продемонстрировать знания.
 Но спринт на этом не заканчивается: впереди ещё одна тема!